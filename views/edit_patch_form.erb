<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/main.css">
</head>
<body>
    <form action="/patches/<%= patch['patch_id'] %>" method="post" id="edit_form">
        <input type="hidden" name="_method" value="put">

        <label for="">Title</label>
        <input type="text" name="patch_name" value="<%= patch['patch_name'] %>">

        <label for="">Description</label>
        <input type="text" name="patch_desc" value="<%= patch['patch_desc'] %>">

        <label for="select_theme">Select a Theme</label>
        <select name="theme_id" form="edit_form" id="select_theme">
            <% themes.each do |theme| %>
                <option value="<%= theme['theme_id'] %>"> 
                    <%= theme['theme_name'] %>
                </option>
            <% end  %>
        </select>

        <button>Submit</button>
    </form>

    <ul>
        <% songs.each do |song| %>
            <li>
                <%= song['song_name'] %>
                <br>
                <%= song['artist'] %>
                <br>
                <%= song['album'] %>
                ,
                <%= song['year'] %>
                <br>
                <br>
                <%= song['song_desc'] %> 
                <a href="/song/<%= song['song_id'] %>/edit">Edit this song</a>
            </li>
         <% end  %>
    </ul>
</body>
</html>